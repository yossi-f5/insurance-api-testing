version: 1.0
runtime: nodejs20
build:
  commands:
    pre-build:
      - npm ci --omit=dev
run:
  runtime-version: 20
  command: npm start
  network:
    port: 3001
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3001"
